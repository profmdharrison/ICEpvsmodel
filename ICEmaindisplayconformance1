# Proof for ICEmaindisplayconformance1
An error occurred while trying to save your proof in PVS.
You might be using an obsolete version of PVS. Please try to re-install PVS with the command `M-x reinstall-pvs`.
The command will open a dialog, select `Download PVS` to install the latest version of PVS.
If the problem persists, please report an issue on [github](https://github.com/nasa/vscode-pvs/issues) or on the [PVS group](https://groups.google.com/g/pvs-group), we will look into it.

## Your proof attempt
Don't panic, your proof attempt for ICEmaindisplayconformance1 is not lost.<br>
VSCode-PVS saved your proof attempt, and you can restore it in Proof Explorer (**...** -> **Restore Proof**).<br>

If everything else fails, below you can find the complete proof script, which you can paste in the prover terminal to repeat the proof:
```lisp
(skosimp*)(beta)(flatten)(lemma "lemma7pce" ("p" "p!1" "d" "d!1" "st" "st!1"))(beta -1)(split -1)(with-labels (flatten -1) ((l7apopmwhdr l7apopmwpn l7apopmwmn l7ncmcpmhdr l7ncmcpmpn l7ncmcpmmn l7ncmcpmpan l7pdsp l7pdsa l7apopnodes l7apodnodes l7apocoord)))(split)(expand "tick_pan_coordinator" 1)(replace "l7ncmcpmhdr" 1)(replace "l7apopnodes" 1)(replace "l7ncmcpmpn" 1)(replace -16 1)(replace "l7ncmcpmmn" 1)(replace "l7apodnodes" 1)(replace -17 1)(replace "l7ncmcpmpan" 1)(replace "l7apocoord" 1)(beta)(expand "act_patient_ok" 1)(lift-if 1)(split 1)(flatten 1)(split 3)(grind)(flatten 1)(split 2)(assert)(flatten 1)(split 2)(flatten 1)(replace -24 1)(expand "setup_p_t_panel" 1)(lift-if 1)(split 1)(flatten 1)(expand "setup_top_panel" 2)(grind)(assert)(assert)(assert)(expand "tick_pan_coordinator" 1)(replace "l7ncmcpmhdr" 1)(replace "l7apopnodes" 1)(replace "l7apodnodes" 1)(replace "l7ncmcpmpn" 1)(replace "l7ncmcpmmn" 1)(replace "l7ncmcpmpan" 1)(replace -16 1)(replace -17 1)(beta)(replace "l7apocoord" 1)(expand "act_patient_ok" 1)(lift-if 1)(split 1)(flatten 1)(split 3)(assert)(flatten 1)(split 2)(assert)(flatten 1)(split 2)(flatten 1)(expand "setup_p_t_panel" 1)(lift-if 1)(split 1)(flatten 1)(expand "setup_top_panel" 2)(replace -29 2)(replace -24 2)(grind)(assert)(flatten 1)(assert)(propax)(propax)(assert)(assert)(assert)(grind)(assert)(propax)(assert)(propax)(propax)(propax)(grind)(grind)(grind)(grind)(grind)(grind)(grind)(grind)
```